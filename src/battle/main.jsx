import React from 'react';
import { createRoot } from 'react-dom/client';
import BattleArena from './BattleArena';

const root = createRoot(document.getElementById('root'));
root.render(<BattleArena />);
